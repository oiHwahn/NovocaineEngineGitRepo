#version 460 core 

in vec3 VertexColour;
out vec4 FragColour;

void main() {
	// r (red), g (green), b (blue), a (alpha)
	// this values range from 0.0f to 1.0f

	FragColour = vec4(VertexColour, 1.0f);
}